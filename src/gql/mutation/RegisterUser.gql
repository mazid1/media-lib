mutation RegisterUser($name: String!, $email: Email!, $passwordHash: String!) {
  userCreate(
    input: { name: $name, email: $email, passwordHash: $passwordHash }
  ) {
    user {
      id
      name
      email
    }
  }
}
